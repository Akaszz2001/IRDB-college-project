{{!-- <table class="table mt-5">
  <thead>
    <tr>
      <th scope="col">No</th>
      <th scope="col">Movie</th>
      <th scope="col">Description</th>
      <th scope="col">Image</th>
    </tr>
  </thead>
      
     
  <tbody>
      {{#each products}}
    <tr>
      <th  scope="row">{{@index}}</th>
      <td>{{this.Name}}</td>
      <td>{{this.Description}}</td> 
      <td><img style="width: 50px; height: 50px;" src="/product-images/{{this.file}}"  alt=""></img></td>
      <td><a href="/admin/edit-movie/{{this._id}}" class="btn btn-primary">Update</a>
          <a href="/admin/delete-product/{{this._id}}" class="btn btn-danger" onclick="return confirm('Do you want to delete {{this.Name}}?')">Delete</a>
      </td>
    </tr>
    {{/each}}

  </tbody>
</table> --}}